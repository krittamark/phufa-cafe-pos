{
  "name": "phufa-cafe-pos-backend",
  "version": "1.0.0",
  "description": "The server-side application providing the API endpoints, business logic, and data management for the Phufa Cafe Point of Sale (POS) system.",
  "main": "src/server.js",
  "scripts": {
    "dev": "nodemon src/server.js",
    "build": "NODE_ENV=production node server.js"
  },
  "license": "MIT",
  "private": true,
  "dependencies": {
    "express": "^5.1.0",
    "phufa-cafe-pos-backend": "file:"
  }
}
